+PART[probeCoreHex]
{
    @name = probeCoreHex_Squad_SYB
    @author = ScrapyardBob
    @title = Probodobodyne HECS (SYB)
    @manufacturer = Scrapyard Bob
    @description = Heavier version of the standard Probodobodyne HECS, with more battery capacity.

    @entryCost += 5000 // for battery upgrade
    @cost += 2640 // 880 (x2 batts)
    @mass += 0.15 // 0.05t (x2 batts)

    %RESOURCE[ElectricCharge]
    {
        @amount = 3000
        @maxAmount = 3000
    }
}